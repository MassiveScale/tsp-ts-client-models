import { Observable } from "rxjs";
import { RxHttpClient } from "./ApiClientRx.js";
import { type RequestOptions } from "./ApiClient.js";
import { {{endpointsClassName}} } from "../endpoints/{{endpointsClassName}}.js";
{{#if modelImports.length}}
import type { {{#each modelImports}}{{this}}{{#unless @last}}, {{/unless}}{{/each}} } from "../models.js";
{{/if}}
export class {{className}} extends RxHttpClient {
{{#each methods}}
{{renderDoc doc "  "}}  {{name}}({{methodParams}}): Observable<{{responseType}}> {
    {{methodBodyObservable}}
  }
{{/each}}
}
